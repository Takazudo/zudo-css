---
import CssPreviewReact from '../css-preview/css-preview';

interface Props {
  html: string;
  css: string;
  title?: string;
  height?: number;
  defaultOpen?: boolean;
}

const props = Astro.props;
---
<CssPreviewReact client:load {...props} />
